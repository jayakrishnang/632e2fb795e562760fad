<div class="row">
    <div class="col-sm-8">
        <div class="row">
            <div class="col-sm-12">
                <h1>Latest Shows</h1>
            </div>
        </div>
        <% @shows.each do |show| %>
            <div class="row">
                <div class="col-sm-12">
                    <%= render partial: 'shows/show_card', locals: { show: show } %>
                </div>
            </div>
        <% end %>
    </div>
</div>
